<div class='container'>
  <div class='row'>
    <div class='col-md-12'>
      <%= search_form_for @q, url: calculations_operator_requests_url, class: 'form-inline' do |f| %>
          <div class='form-group'>
            <%= f.text_field :date_gteq, id: 'gteq', class:'form-control', placeholder: 'Дата от' %>
          </div>
          <div class='form-group'>
            <%= f.text_field :date_lteq, id: 'lteq', class:'form-control', placeholder: 'до' %>
          </div>

          <%= f.submit 'Поиск', class: 'btn btn-primary' %>
      <% end %>
    </div>
  </div>
  <div class='row'>
    <div class='col-md-12'>
      <%- if @info%>
          <blockquote>
              <h3><b>Интенсивность &lambda;: </b><%= @info[:lambda].round(3)%>(чел.)</h3>
              <h3><b>Интенсивность обслуживания требований &mu;: </b><%= @info[:mu].round(0)%>(чел./час)</h3>
              <h3><b>Загрузка системы &rho;: </b><%= @info[:ro].round(3)%></h3>
              <h3><b>Вероятность того, что в системе будет N требований Pn: </b><%= @info[:piN].round(3)%></h3>
              <h3><b>Среднее количество клиентов M: </b><%= @info[:em].round(3)%>(чел.)</h3>
              <h3><b>Среднее время обслуживания клиентов операторами W1: </b><%= (@info[:w1]*60).round(0)%>(мин.)</h3>
          </blockquote>
      <% else %>
          <h2>Задайте поиск</h2>
      <% end %>
    </div>
  </div>
</div>